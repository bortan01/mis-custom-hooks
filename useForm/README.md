# useForm Hook

Ejemplo de uso:
```
   const useForm = {
      name  : "NOMBRE",
      age   : 0,
      email : "email@ejemplo.com"
   };
   cont [ values, handleInputChange, reset ]= useForm (initialForm);
```
